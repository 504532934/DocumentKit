services:
  documentkit:
    build: .
    ports:
      - '127.0.0.1:3000:3000'
    environment:
      DOCUMENTKIT_HOST: 0.0.0.0
      DOCUMENTKIT_API_KEY: ${DOCUMENTKIT_API_KEY:?Set DOCUMENTKIT_API_KEY}
    init: true
    restart: unless-stopped
